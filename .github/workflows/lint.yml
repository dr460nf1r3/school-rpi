name: Test code with shellcheck

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  acls:
    runs-on: ubuntu-latest

    steps:
      - name: ShellCheck
        uses: ludeeus/action-shellcheck@2.0.0
    